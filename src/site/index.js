/**
 * @file
 * @author
 * @since
 */
import './styles.scss';

import { csv, json } from 'd3-fetch';

// import other files here

(async () => {
  const resize = () => {};

  window.addEventListener('resize', () => {
    resize();
  });

  resize();
})();
